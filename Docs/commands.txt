/start		-- инициирует диалог/вызывает реплику 1
/delete_me	-- удал€ет id пользовател€ из базы данных + вызывает реплику 2
*/add_event	-- (только староста) добавл€ет внутриклассовое событие, которое автоматически рассылаетс€ всем ученикам из этого класса
*/add_homework	-- (только староста) добавл€ет дз
/get_number	-- ждЄт на ввод ‘», выводит номер телефона
*/change_info	-- (только староста) измен€ет информацию выбранного человека из класса
/change_info	-- измен€ет свою информацию
/next_duty	-- показывает дату следующего дежурства и количество дней до него